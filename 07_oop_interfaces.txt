3.1

KMesh1D.FromLinSpace(coord, start, stop, num)
// создание сетки в одномерном k-пространстве аналогично numpy.linspace

KMesh1D.FromArray(coord, values)
// создание сетки в одномерном k-пространстве из массива значений

KMesh2D.FromProduct(mesh1, mesh2)
// создание сетки в двумерном k-пространстве в виде декартова произведения одномерных сеток


3.2

FunctionCollection
// абстрактный класс для набора функций, используемого в качестве базиса

ProgressObserver
// абстрактный класс, обрабатывающий данные о степени завершённости вычислений
